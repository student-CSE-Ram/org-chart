# Server Port (Render automatically sets PORT in production)
PORT=5000

# MongoDB Connection String (e.g. MongoDB Atlas connection string for Render)
MONGODB_URI=mongodb://127.0.0.1:27017/org_chart

# Frontend Client URL for CORS (your Vercel application URL in production)
CLIENT_URL=http://localhost:5173
